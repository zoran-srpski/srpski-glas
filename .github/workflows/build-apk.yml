name: Build Android APK

on:
  push:
    branches: [ main, test/local-whisper, test/start-beep-only ]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout source
        uses: actions/checkout@v4

      - name: Set up Java 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Apply start-beep-only test patch
        if: github.ref == 'refs/heads/test/start-beep-only'
        run: python3 tools/enable_start_beep_only_test.py

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.9'

      - name: Build debug APK
        run: gradle --no-daemon :app:assembleDebug

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: srpski-glas-debug-apk
          path: app/build/outputs/apk/debug/app-debug.apk
          if-no-files-found: error
